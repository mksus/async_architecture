from django.urls import path
# from billing.views import TransactionList, GetBalance


urlpatterns = [
    # path('transactions/', TransactionList.as_view()),
    # path('balance/', GetBalance.as_view()),
]
